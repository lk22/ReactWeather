var React = require('react');

const Weathertitle = (props) => {
	var styles = {
		textAlign: 'center'
	};

	return ( <h1 className="text-center page-title">Weather</h1> );
};

module.exports = Weathertitle;